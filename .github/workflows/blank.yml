name: CI Test Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Say Hello from DevOps Guru
        run: echo "🚀 Hello from GitHub Actions!"
